spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/market_plus_test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.enabled=false

app.jwt.secret=${APP_JWT_SECRET:test-secret-key-for-ci-minimum-32-characters-long}
app.jwt.expiration-ms=86400000


# Stripe — valeurs factices pour les tests
stripe.secret-key=sk_test_fake_key_for_tests
stripe.webhook-secret=whsec_fake_secret_for_tests

# MoMo — valeurs factices pour les tests
momo.api-url=https://sandbox.momodeveloper.mtn.com
momo.subscription-key=fake_momo_key_for_tests